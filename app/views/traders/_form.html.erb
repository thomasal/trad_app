<%= form_for(@trader) do |f| %>
  <% if @trader.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trader.errors.count, "error") %> prohibited this trader from being saved:</h2>

      <ul>
      <% @trader.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nom_trader %><br>
    <%= f.text_field :nom_trader %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
